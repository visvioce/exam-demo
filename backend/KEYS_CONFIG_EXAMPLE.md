# ========================================
# 环境变量配置示例
# - 系统启动时会验证这些密钥配置
# - 必须修改为强随机生成的密钥！
# ========================================

# JWT 密钥配置
# ⚠️ 必须修改为随机生成的强密钥！
# 生成方法：
#   JWT_SECRET=$(openssl rand -base64 64)

# AES 加密密钥
# ⚠️ 必须修改为随机生成的强密钥！
# 生成方法：
#   AES_SECRET=$(openssl rand -base64 64)

# 示例强密钥（请勿使用！）：
# JWT_SECRET=9K7x3h5v2y8m4q3w0e8t9s7j6k5n4x
# AES_SECRET=K9k8j7m4q0w8e1t9s7j6k5n4x

# 前端开发服务器
VITE_PORT=5173

# 后端服务端口
SERVER_PORT=8080