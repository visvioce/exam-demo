# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=exam_system
DB_USERNAME=root
DB_PASSWORD=your_password_here

# JWT配置 (生产环境请使用至少256位的随机字符串)
JWT_SECRET=your_jwt_secret_key_at_least_256_bits_long_random_string

# CORS配置 (前端地址，多个用逗号分隔)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# 环境变量配置模板
